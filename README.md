#libds

## Data Structures

1. Queue 
    - void initQueue(queue *q);
    - int isEmpty(queue *q);
    - int isFull(queue *q);
    - void enqueue(queue *q, int value);
    - int dequeue(queue *q);
    - void printq(queue *q);

2. ...
